# EX01 채점 기준 · 체크리스트 (100점 / 합격선 60)

> NCS 애플리케이션 배포(2001020214_23v6) 수행준거 기반
> 확인 단위: 파일·스크립트·실행결과·배포_결과보고서

---

## 1. 애플리케이션 배포환경 구성하기 — 능력단위요소 ① (20점)

| 수행준거 | 확인 항목 | 확인 위치 | 배점 |
|:--|:--|:--|--:|
| 빌드·배포 환경 구성 방안을 계획할 수 있다 | JDK 21·Spring Boot 3.5 빌드 환경을 계획·기록 | build.gradle / 배포_결과보고서(1.환경) | 5 |
| 배포를 위한 도구와 시스템을 결정할 수 있다 | 빌드=Gradle(bootJar), 실행=java -jar, 프로파일=prod 방식 결정 | build.gradle / 배포_결과보고서 | 4 |
| 결정한 도구와 시스템을 설치할 수 있다 | gradlew(wrapper)·releases 배포 디렉토리 구성 | gradlew / releases | 5 |
| 상세 구성 및 설정을 할 수 있다 | prod 상세 설정(포트 9090·env=production·로그레벨) | application-prod.properties | 6 |

## 2. 애플리케이션 소스 검증하기 — 능력단위요소 ② (25점)

| 수행준거 | 확인 항목 | 확인 위치 | 배점 |
|:--|:--|:--|--:|
| 형상관리 서버에서 소스코드를 체크아웃 할 수 있다 | git clone/checkout 로 소스 확보·이력 확인 | git 로그 / 배포_결과보고서 | 6 |
| 검증 도구로 라이브러리·소스·로직 오류를 검증할 수 있다 | `gradlew test` 로 헬스/버전 로직 검증(통과) | HealthControllerTest / test 실행 로그 | 13 |
| 환경 설정·운영 환경·대상 시스템 정보 오류를 확인할 수 있다 | 포트·프로파일·버전 값이 명세와 일치하는지 점검 | application(-prod).properties / 배포_결과보고서(2.검증) | 6 |

## 3. 애플리케이션 빌드하기 — 능력단위요소 ③ (30점)

| 수행준거 | 확인 항목 | 확인 위치 | 배점 |
|:--|:--|:--|--:|
| 검증 결과 문제 없으면 빌드 시스템으로 이관할 수 있다 | 검증 통과 후 `clean` 으로 빌드 준비 | build.sh / 실행 로그 | 4 |
| 빌드 절차에 따른 빌드 스크립트를 작성할 수 있다 | 빌드 스크립트 작성(clean→test→bootJar→릴리스 보관) | scripts/build.sh | 8 |
| 빌드 스크립트·도구로 빌드를 실행할 수 있다 | `gradlew clean bootJar` 실행 | build.sh / 실행 로그 | 8 |
| 빌드 실행 결과가 정상 완료되었는지 확인할 수 있다 | `build/libs/app.jar` 생성·릴리스 사본 보관 확인 | build/libs/app.jar / releases/app-1.0.0.jar | 6 |
| 빌드 실패 시 문제·원인을 파악해 설명할 수 있다 | 실패 상황·원인 분석 기록 | 배포_결과보고서(3.빌드) | 4 |

## 4. 애플리케이션 배포하기 — 능력단위요소 ④ (25점)

| 수행준거 | 확인 항목 | 확인 위치 | 배점 |
|:--|:--|:--|--:|
| 애플리케이션 실행 환경 정보를 확인할 수 있다 | 실행 명령·포트(9090)·프로파일(prod) 확인 | run.sh / 배포_결과보고서(4.배포) | 5 |
| 배포 절차에 따라 운영환경에 적용할 수 있다 | prod 프로파일로 실행(java -jar … --spring.profiles.active=prod) | run.sh / 실행 로그 | 7 |
| 배포 후 정상 작동하는지 확인할 수 있다 | `/api/health`·`/actuator/health` = UP 확인·캡처 | /api/health · /actuator/health / 캡처 | 7 |
| 문제 발생 시 이전 상태로 복원할 수 있다 | `rollback.sh` 로 복원 후 `/api/version` 확인 | scripts/rollback.sh / /api/version | 6 |

---

## 배점 합계

| 능력단위요소 | 배점 |
|:--|--:|
| ① 배포환경 구성 | 20 |
| ② 소스 검증 | 25 |
| ③ 빌드 | 30 |
| ④ 배포·롤백 | 25 |
| **합계** | **100** |

> 합격선 60점. 동작 화면 캡처(헬스체크·롤백 후 재확인)와 배포_결과보고서 제출이 확인 근거다.
> 한글 경로에서 `gradlew test`(명령줄)가 클래스 로딩 오류를 내면 IntelliJ 실행기 또는 영문 경로에서 실행한다.

spring.application.name=demo

# 기본(개발) 환경
server.port=8080
app.version=1.0.0
app.env=dev

# Actuator 헬스체크 노출 (배포 후 정상 동작 확인용)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
